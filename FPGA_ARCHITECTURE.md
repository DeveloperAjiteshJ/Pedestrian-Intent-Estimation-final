# TinyMobileNet-XS FPGA Architecture

## System Block Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         NEXYS A7-100T (XC7A100T)                       │
│                                                                         │
│  ┌──────────────┐     ┌────────────────────────────────────────────┐  │
│  │   100 MHz    │────▶│    Inference Core                          │  │
│  │   Clock      │     │  ┌──────────────────────────────────────┐ │  │
│  └──────────────┘     │  │  Control FSM (tinymobilenet_top)     │ │  │
│                       │  │  ┌─────────────────────────────────┐ │ │  │
│  ┌──────────────┐     │  │  │ Layer Pipeline:                 │ │ │  │
│  │ Reset Button │────▶│  │  │ ┌─────────────────────────────┐ │ │ │  │
│  │   (D9, AL)   │     │  │  │ │ Conv1: 64×64×3 → 32×32×8    │ │ │ │  │
│  └──────────────┘     │  │  │ │ (3×3, stride=2, 8 channels) │ │ │ │  │
│                       │  │  │ │                             │ │ │ │  │
│  ┌──────────────┐     │  │  │ │ Bottleneck A/B/C (×2)      │ │ │ │  │
│  │ Start Button │────▶│  │  │ │ Depthwise Separable Convs  │ │ │ │  │
│  │ (C9, BTN1)   │     │  │  │ │                             │ │ │ │  │
│  └──────────────┘     │  │  │ │ TSM: Temporal Shift (0 MACs)│ │ │ │  │
│                       │  │  │ │                             │ │ │ │  │
│  ┌──────────────┐     │  │  │ │ Conv_pw: → 48 channels     │ │ │ │  │
│  │ UART RX      │────▶│  │  │ │                             │ │ │ │  │
│  │ (A9)         │     │  │  │ │ GAP: 8×8×48 → 48-dim vec  │ │ │ │  │
│  └──────────────┘     │  │  │ │                             │ │ │ │  │
│                       │  │  │ │ FC1: 48 → 32 (ReLU)        │ │ │ │  │
│                       │  │  │ │ FC2: 32 → 2 (logits)       │ │ │ │  │
│                       │  │  │ │                             │ │ │ │  │
│                       │  │  │ └─────────────────────────────┘ │ │ │  │
│                       │  │  │                                   │ │ │  │
│                       │  │  │ Output: [logits_0, logits_1]    │ │ │  │
│                       │  │  │   ▼                              │ │ │  │
│                       │  │  │ Argmax → predicted_class        │ │ │  │
│                       │  │  │ Softmax → confidence_score      │ │ │  │
│                       │  │  └─────────────────────────────────┘ │ │  │
│                       │  └──────────────────────────────────────┘ │  │
│                       └────────────────────────────────────────────┘  │
│                                        │                              │
│                                        ▼                              │
│       ┌───────────────┬─────────────┬──────────────┐                 │
│       │               │             │              │                 │
│  ┌────▼────┐  ┌──────▼──────┐  ┌──▼──────┐  ┌───▼──────┐            │
│  │ LED Out │  │ LED Out     │  │ State   │  │ UART TX  │            │
│  │ Class   │  │ Confidence  │  │ Debug   │  │ (B10)    │            │
│  │ (2 LEDs)│  │ (8 LEDs)    │  │ (4 LEDs)│  │          │            │
│  └────┬────┘  └─────┬──────┘  └────┬────┘  └────┬─────┘            │
│       │              │              │            │                  │
└───────┼──────────────┼──────────────┼────────────┼──────────────────┘
        │              │              │            │
        ▼              ▼              ▼            ▼
    ┌─────┐      ┌────────┐     ┌───────┐   ┌──────────┐
    │LED  │      │ LED    │     │LED    │   │ Serial   │
    │[1:0]│      │[7:0]   │     │[3:0]  │   │ to PC    │
    └─────┘      └────────┘     └───────┘   └──────────┘
    Green/Red    Confidence     FSM State    @115200baud
```

---

## Memory Architecture

```
┌─────────────────────────────────────────────────────┐
│              FPGA Memory Resources                  │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ BRAM (Block RAM) - 270 blocks × 36Kb each   │   │
│  │ Total: ~9.7 MB                              │   │
│  ├─────────────────────────────────────────────┤   │
│  │ ┌────────────────────────────┐              │   │
│  │ │ Weights (INT8):      ~7 KB │              │   │
│  │ │ ┌─────────────────────────┐│              │   │
│  │ │ │ Conv1, Conv_pw, FC      ││              │   │
│  │ │ │ Bottleneck weights      ││              │   │
│  │ │ │ ~7,232 parameters       ││              │   │
│  │ │ └─────────────────────────┘│              │   │
│  │ └────────────────────────────┘              │   │
│  │                                             │   │
│  │ ┌────────────────────────────┐              │   │
│  │ │ Test Sample #0:     ~49 KB │              │   │
│  │ │ 4 frames × 64×64×3 UINT8   │              │   │
│  │ │ (4 × 3 × 64 × 64 = 49,152B)│              │   │
│  │ └────────────────────────────┘              │   │
│  │                                             │   │
│  │ ┌────────────────────────────┐              │   │
│  │ │ Activation Buffer:   ~50 KB │              │   │
│  │ │ (48×8×8 features, FP32)     │              │   │
│  │ └────────────────────────────┘              │   │
│  │                                             │   │
│  │ Total used: ~106 KB / 9.7 MB = 1.1% ✅      │   │
│  │ Remaining: 9.6 MB available                 │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ Distributed RAM (LUTs used as memory):       │   │
│  │ ┌────────────────────────────────────────┐  │   │
│  │ │ Feature Maps (intermediate):  ~10 KB   │  │   │
│  │ │ Accumulators:                ~5 KB     │  │   │
│  │ └────────────────────────────────────────┘  │   │
│  │ Total: ~15 KB                               │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ DDR3 (Optional - External Memory):          │   │
│  │ ┌────────────────────────────────────────┐  │   │
│  │ │ Additional test samples (1-15):        │  │   │
│  │ │ 15 × 49 KB = ~735 KB                   │  │   │
│  │ │ (Not used in basic implementation)     │  │   │
│  │ └────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘

Memory Utilization Summary:
├─ BRAM:        106 KB used / 9.7 MB available (1.1%) ✅
├─ LUT RAM:      15 KB (part of LUT budget)
├─ DDR3:      Optional (if extended test set needed)
└─ Total:      Very efficient for this model! 🎯
```

---

## Data Flow Pipeline

```
Time: t=0 to t=T (where T = total inference latency)

Frame Input (4 frames):
┌──────────┬──────────┬──────────┬──────────┐
│ Frame 0  │ Frame 1  │ Frame 2  │ Frame 3  │
│ 64×64×3  │ 64×64×3  │ 64×64×3  │ 64×64×3  │
└──────────┴──────────┴──────────┴──────────┘
    │          │          │          │
    ▼          ▼          ▼          ▼
┌─────────────────────────────────────────┐
│  Conv1: 3×3, stride=2, 8 channels       │
│  Input: 64×64×3 → Output: 32×32×8       │
│  Latency: ~10 cycles per frame          │
│  Total: ~40 cycles for 4 frames         │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Bottleneck A: DW 3×3, no stride         │
│ Input: 32×32×8 → Output: 32×32×8        │
│ Latency: ~20 cycles                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Bottleneck B: DW 3×3, stride=2          │
│ Input: 32×32×8 → Output: 16×16×12       │
│ Latency: ~30 cycles                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Bottleneck C (×2): stride=2, then 1     │
│ Input: 16×16×12 → Output: 8×8×16        │
│ Latency: ~50 cycles                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ TSM: Temporal Shift Module               │
│ (Address remapping, 0 arithmetic cost)   │
│ Latency: ~1 cycle                       │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Conv_pw: 1×1 expansion 16→48             │
│ Input: 8×8×16 → Output: 8×8×48           │
│ Latency: ~10 cycles                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Global Average Pool: 8×8×48 → 48        │
│ Latency: ~100 cycles (sum + divide)     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ FC1: 48 → 32 (ReLU)                     │
│ Matrix-vector multiply                  │
│ Latency: ~100 cycles                    │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ FC2: 32 → 2 (logits)                    │
│ Latency: ~50 cycles                     │
└─────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────┐
│ Softmax + Argmax (post-processing)      │
│ Compute exp, sum, divide, argmax        │
│ Latency: ~100 cycles                    │
└─────────────────────────────────────────┘
    │
    ▼
┌──────────────────────────────────────────┐
│ OUTPUT                                   │
│ class_led[1:0]     ← predicted class     │
│ confidence_led[7:0] ← confidence score   │
└──────────────────────────────────────────┘

Total Latency Estimate:
├─ Conv layers:  ~150 cycles
├─ TSM:           ~1 cycle
├─ GAP:         ~100 cycles
├─ FC layers:   ~250 cycles  ← Bottleneck (can parallelize)
├─ Post-proc:   ~100 cycles
└─ TOTAL:       ~600 cycles @ 100 MHz = ~6 µs (compute only)
  + ~1-10 ms overhead (memory stalls, control)
  = ~1-10 ms per inference (realistic estimate)
```

---

## Control FSM State Diagram

```
                    ┌─────────┐
                    │ IDLE    │
                    │ (S0)    │◄────────┐
                    └────┬────┘         │
                         │             │
                         ▼             │
                    ┌─────────┐         │
                    │ WAIT    │         │
        ┌──────────►│ BUTTON  │         │
        │           │ (S1)    │         │
        │           └────┬────┘         │
        │                │             │
        │ start_button=1 │             │
        │                ▼             │
        │           ┌─────────┐         │
        │           │ LOAD    │         │
        │           │ DATA    │         │
        │           │ (S2)    │         │
        │           └────┬────┘         │
        │                │             │
        │                ▼             │
        │           ┌─────────┐         │
        │           │INFERENCE│         │
        │           │ (S3)    │         │
        │           └────┬────┘         │
        │                │             │
        │  inference_done│             │
        │                ▼             │
        │           ┌─────────┐         │
        │           │ OUTPUT  │         │
        │           │ (S4)    │         │
        │           └────┬────┘         │
        │                │             │
        │                ▼             │
        │           ┌─────────┐         │
        │           │ UART_TX │         │
        │           │ (S5)    │         │
        │           └────┬────┘         │
        └───────────────┘

States:
S0 IDLE:     Initialize, wait for button
S1 WAIT:     Button debounce, wait for press
S2 LOAD:     Load test data from BRAM
S3 INFERENCE: Execute layer pipeline
S4 OUTPUT:   Drive LEDs with results
S5 UART_TX:  Send results to PC via UART
(then loop back to S1)

Transitions:
IDLE → WAIT:       Automatic
WAIT → LOAD:       start_button = 1
LOAD → INFERENCE:  Data loaded
INFERENCE → OUTPUT: inference_done = 1
OUTPUT → UART_TX:  Results ready
UART_TX → WAIT:    TX complete
```

---

## Pin Assignment Summary

```
NEXYS A7-100T DDR4 (XC7A100T CSG324)

INPUT PINS:
├─ E3   │ clk         │ 100 MHz system clock
├─ D9   │ rst_n       │ Reset button (active low)
├─ C9   │ start_button│ BTN1 (inference trigger)
└─ A9   │ uart_rx     │ Serial RX from PC

OUTPUT PINS:
├─ H17  │ class_led[1]│ Green LED (Crossing)
├─ K15  │ class_led[0]│ Red LED (Not-Crossing)
├─ J15  │ confidence_led[7]│ Confidence bit 7 (MSB)
├─ J13  │ confidence_led[6]│
├─ H13  │ confidence_led[5]│
├─ G13  │ confidence_led[4]│
├─ B11  │ confidence_led[3]│
├─ A11  │ confidence_led[2]│
├─ D12  │ confidence_led[1]│
├─ D11  │ confidence_led[0]│ Confidence bit 0 (LSB)
├─ E15  │ state_led[3]│ FSM state bit 3 (debug)
├─ F15  │ state_led[2]│ FSM state bit 2
├─ G15  │ state_led[1]│ FSM state bit 1
├─ H15  │ state_led[0]│ FSM state bit 0
├─ E14  │ inference_done│ Inference complete flag
└─ B10  │ uart_tx     │ Serial TX to PC

BRAM USAGE:
├─ 20/270 blocks used
├─ Size: ~106 KB / 9.7 MB (1.1%)
└─ Contains: Weights + test vectors

TIMING:
├─ Clock:  100 MHz (10 ns period)
├─ Setup:  Required time < 8 ns
├─ Hold:   Required time > -2 ns
└─ Status: All paths should close ✅
```

---

## Resource Utilization Estimate

```
┌──────────────────────────────────────────────────────┐
│ FPGA Resource Usage (XC7A100T)                       │
├──────────────────────────────────────────────────────┤
│                                                      │
│ LUTs (Lookup Tables):                               │
│ ├─ Available:    15,850                             │
│ ├─ Estimated:    ~1,500-2,000 (Conv logic)         │
│ ├─ Utilization:  ~10-13%                            │
│ └─ Status:       ✅ Very Low (plenty of room)       │
│                                                      │
│ Flip-Flops (FFs):                                   │
│ ├─ Available:    31,700                             │
│ ├─ Estimated:    ~1,000-1,500 (pipelines)          │
│ ├─ Utilization:  ~3-5%                              │
│ └─ Status:       ✅ Very Low (excellent)            │
│                                                      │
│ Block RAM (BRAM):                                   │
│ ├─ Available:    270 × 36Kb = ~9.7 MB              │
│ ├─ Estimated:    ~20 blocks (~720 KB)              │
│ ├─ Utilization:  ~7%                                │
│ └─ Status:       ✅ Good (can store all test data)   │
│                                                      │
│ DSP48E1 (Multiply-Accumulate):                      │
│ ├─ Available:    240                                │
│ ├─ Estimated:    ~16-32 (FC layers)                │
│ ├─ Utilization:  ~7-13%                             │
│ └─ Status:       ✅ Low (can parallelize more)      │
│                                                      │
│ I/O Pins:                                           │
│ ├─ Available:    210                                │
│ ├─ Used:         ~20 (clock, LEDs, UART, buttons)  │
│ ├─ Utilization:  ~10%                               │
│ └─ Status:       ✅ Very Low                        │
│                                                      │
│ Overall Summary:                                     │
│ ├─ Design Margin: ~70-80% headroom                  │
│ ├─ Build Speed:   ~5-10 minutes                     │
│ ├─ Place & Route: Should close timing easily        │
│ └─ Status:       ✅ EXCELLENT (very conservative)   │
│                                                      │
└──────────────────────────────────────────────────────┘
```

---

**This architecture is optimized for:**
- ✅ **Low resource utilization** (lots of headroom for optimization)
- ✅ **Streaming data** (one sample at a time from UART)
- ✅ **LED feedback** (immediate visual output)
- ✅ **Pipelined inference** (high throughput potential)
- ✅ **Easy integration** (button-press triggered inference)

**Diagram generated for visualization purposes.**
